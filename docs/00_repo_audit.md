# Hermes_Retek — первичный аудит

Дата: 2026-06-12

Репозиторий доступен как `megawinrar/Hermes_Retek`, ветка `main`.

На момент проверки README выглядит как стандартный шаблон после переноса из GitLab. Рабочие конфиги Hermes, agents, providers, skills, Docker/compose и routing-файлы через доступный поиск не обнаружены.

Проверены запросы: `hermes`, `agent`, `skill`, `HSS`, `classify`, `token`.

Также вручную проверены типовые пути: `config.yaml`, `config.yml`, `docker-compose.yml`, `package.json`, `pyproject.toml`, `skills/README.md`.

Вывод: сценарии ниже добавлены как проектная основа. После загрузки фактического кода их нужно привязать к реальным entrypoint-файлам.
